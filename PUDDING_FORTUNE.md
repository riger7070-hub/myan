# 🍮 운기 푸딩 · 오늘의 행운 (일회용 QR)

푸딩 가게를 위한 포춘쿠키 스타일의 행운 메시지 기능입니다.
**푸딩 1개당 일회용 QR 코드 1장**을 스티커 라벨로 부착해, 구매 고객만 행운 메시지를 볼 수 있습니다.

## 📋 기능 개요

### 1. 일회용 QR 라벨 생성기 (사장님용)
- **URL**: `/pudding-qr-batch`
- **기능**:
  - 관리자 PIN 인증 후 일회용 코드 일괄 생성 (1~100개)
  - 스티커 라벨 크기 선택 (40×40 / 50×50 / 50×30 / 60×40 mm)
  - 브라우저 인쇄 (라벨 1장 = 용지 1장, `@page` 크기 자동 설정)
  - PNG 일괄 저장 (라벨 프린터 전용 앱으로 인쇄 시)

### 2. 오늘의 행운 페이지 (고객용)
- **URL**: `/pudding-fortune?c=코드`
- **기능**:
  - QR 코드 없이 접속 시 잠금 화면 (구매 유도)
  - 일회용 코드 검증 — 푸딩 1개당 행운 메시지 1회
  - 첫 스캔 후 24시간 동안은 같은 메시지 재확인 가능
  - **4개국어 지원** (한국어/영어/중국어/일본어, 브라우저 언어 자동 감지 + 수동 전환)
  - 6가지 카테고리 × 4개 메시지 (총 24개, 언어별 번역 완비)
  - AI 사주 풀이로 자동 유도

## 🔐 일회용 코드 시스템

| 항목 | 내용 |
|------|------|
| 코드 형식 | 10자리 (혼동 문자 I,O,0,1 제외 32자) — 추측 불가 |
| 저장 | D1 `fortune_codes` 테이블 |
| 사용 처리 | 첫 스캔 시 `used_at` + `fortune_seed` 확정 (원자적 UPDATE로 동시 스캔 경합 방지) |
| 재확인 | 사용 후 24시간 이내 같은 메시지 표시, 이후 "이미 사용됨" |
| 생성 인증 | `UNGI_PIN` 환경변수 (기본값 운기 매장 PIN과 동일) |
| Rate Limit | IP당 분당 30회 (`RL_API`) |

### API
- `POST /api/fortune-qr/generate` — `{ pin, count }` → `{ batchId, codes[] }`
- `POST /api/fortune-qr/redeem` — `{ code }` → `{ seed, revisit }` / 404(무효) / 410(사용됨)

## 🎯 사용 방법

### 가게 주인용 (라벨 인쇄)
1. `/pudding-qr-batch` 접속
2. PIN 입력 + 수량 선택 → "QR 코드 생성"
3. 라벨 크기 선택 후 "🖨️ 라벨 인쇄" (또는 PNG 저장 후 라벨 프린터 앱으로 인쇄)
4. 푸딩 포장에 스티커 부착

### 고객용 (행운 확인)
1. 푸딩에 붙은 QR 코드 스캔
2. 오늘의 행운 메시지 확인 (푸딩 1개당 1회)
3. 24시간 내 재스캔 시 같은 메시지 재확인 가능

## 🔧 기술 사양

- **프론트엔드**: Vanilla JS (의존성 최소화)
- **QR 생성**: qrcodejs 라이브러리
- **제한 방식**: 서버 검증 (D1 일회용 코드) — localStorage 아님
- **다국어**: ko / en / zh / ja
- **배포**: Cloudflare Workers + D1

## 📦 배포하기

```bash
npx wrangler deploy
```

## 🎯 향후 개선 가능 사항

- [ ] 배치별 사용률 통계 (몇 % 스캔됐는지)
- [ ] 계절별/시간대별 메시지 변화
- [ ] 메시지 공유 기능
- [ ] 행운 메시지 통계 대시보드

## 💡 커스터마이징

메시지를 추가하거나 변경하려면 `pudding-fortune.html`의 `FORTUNES` 배열을 수정하세요.
각 메시지는 4개국어 객체입니다:

```javascript
{ ko: '메시지', en: 'Message', zh: '消息', ja: 'メッセージ' }
```

> ⚠️ 메시지 순서/개수를 바꾸면 이미 사용된 코드의 시드 매핑이 달라질 수 있으니
> 기존 메시지는 유지하고 **뒤에 추가**하는 방식을 권장합니다.

---

**운기 푸딩과 함께 행복한 하루 되세요! 💛**
