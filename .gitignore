# Wrangler 로컬 캐시 (절대 올리면 안 됨)
.wrangler/

# 실수로 생성된 빈 파일
main

# 환경변수 파일 (혹시 생성 시 대비)
.env
.env.local
.dev.vars

# OS 파일
.DS_Store
Thumbs.db

# 노드 모듈
node_modules/

# 빌드 출력
dist/

# 테스트가 worker.js 를 .mjs 로 임시 복사하는 자리 (test/load-worker.mjs 참고)
test/.tmp/

# Lock 파일 (package-lock.json은 배포 환경에 필요하므로 포함)
*.lock

# 아카이브 (git에 불필요)
myan-icons.zip

# 이 프로젝트와 무관한 별도 작업 폴더 (실수로 커밋됐던 것 제거, 재추가 방지)
buyma/
